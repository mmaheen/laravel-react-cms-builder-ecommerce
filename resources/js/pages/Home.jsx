import React from "react";
export default function Home({ title }) {
    return (
        <div>
            <h1>{title}</h1>
            <p>This is Home page (React inside Blade)</p>
        </div>
    );
}
